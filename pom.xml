<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.thortech</groupId>
    <artifactId>thorcash</artifactId>
    <version>1.0.0</version>
    <name>ThorCash</name>
    <description>ThorCash Financial Management System</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>
        <javafx.version>21.0.6</javafx.version>
        <!-- Platform classifier: must be set to 'win' on Windows build hosts -->
        <javafx.platform>win</javafx.platform>

        <main.class>com.schaccs.Launcher</main.class>
        <app.name>ThorCash</app.name>
        <app.vendor>Thor Technologies</app.vendor>
        <app.version>${project.version}</app.version>
    </properties>

    <dependencies>
        <!-- NOTE: platform classifier is REQUIRED for JavaFX platform artifacts -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
            <classifier>${javafx.platform}</classifier>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
            <classifier>${javafx.platform}</classifier>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
            <version>${javafx.version}</version>
            <classifier>${javafx.platform}</classifier>
        </dependency>

        <!-- other dependencies unchanged -->
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.49.1.0</version>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>5.4.1</version>
        </dependency>
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>3.0.3</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.11.4</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
            <version>6.2.1</version>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.7.4</version>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>9.2.0</version>
        </dependency>
        <dependency>
            <groupId>org.mariadb.jdbc</groupId>
            <artifactId>mariadb-java-client</artifactId>
            <version>3.5.1</version>
        </dependency>

        <!-- SLF4J binding — routes SLF4J calls to java.util.logging -->
        <!-- Ikonli: FontAwesome5 glyph icons for sidebar navigation -->
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-javafx</artifactId>
            <version>12.3.1</version>
        </dependency>
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-fontawesome5-pack</artifactId>
            <version>12.3.1</version>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-jdk14</artifactId>
            <version>1.7.36</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Log4j-to-SLF4J bridge — routes Log4j API calls (from POI) to SLF4J -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-to-slf4j</artifactId>
            <version>2.24.3</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <!-- version.properties is filtered to inject ${project.version} at build time.
             The UpdateService reads this file at runtime to know the current version. -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>version.properties</include>
                </includes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>version.properties</exclude>
                </excludes>
            </resource>
        </resources>

        <plugins>
            <!-- Compile with Java 21, warn on unchecked operations -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <compilerArgs>
                        <arg>-Xlint:unchecked</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <!-- Run unit tests via JUnit 5 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
            </plugin>

            <!-- Copy all runtime-scope dependencies into target/libs/ for the installer -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <!-- preserve original copy to target/libs -->
                    <execution>
                        <id>copy-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/libs</outputDirectory>
                            <includeScope>runtime</includeScope>
                        </configuration>
                    </execution>

                    <!-- COPY dependencies to installer-input so jpackage input can find them -->
                    <execution>
                        <id>copy-dependencies-installer-input</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/installer-input</outputDirectory>
                            <includeScope>runtime</includeScope>
                        </configuration>
                    </execution>

                    <!-- Copy the main artifact into installer-input -->
                    <execution>
                        <id>copy-main-jar-to-installer-input</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>${project.groupId}</groupId>
                                    <artifactId>${project.artifactId}</artifactId>
                                    <version>${project.version}</version>
                                    <outputDirectory>${project.build.directory}/installer-input</outputDirectory>
                                    <destFileName>${project.build.finalName}.jar</destFileName>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Build the executable JAR with Class-Path and Main-Class manifest entries -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <addClasspath>true</addClasspath>
                            <classpathPrefix>libs/</classpathPrefix>
                            <mainClass>${main.class}</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <!-- Run with: mvn javafx:run -->
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>${main.class}</mainClass>
                </configuration>
            </plugin>

            <!-- jlink: create a custom JRE runtime image with needed JDK + JavaFX modules.
                 We unpack javafx-jmods above and pass that folder on the module-path. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jlink-plugin</artifactId>
                <version>3.2.0</version>
                <configuration>
                    <output>${project.build.directory}/runtime</output>

                    <!-- Add jmods module-path: the JDK jmods (JavaFX runs from its platform JARs on the classpath) -->
                    <modulePaths>
                        <modulePath>${java.home}/jmods</modulePath>
                    </modulePaths>

                    <addModules>
                        <!-- JDK modules your app requires (keep in sync with build-installer.bat) -->
                        <addModule>java.base</addModule>
                        <addModule>java.desktop</addModule>
                        <addModule>java.logging</addModule>
                        <addModule>java.management</addModule>
                        <addModule>java.naming</addModule>
                        <addModule>java.net.http</addModule>
                        <addModule>java.prefs</addModule>
                        <addModule>java.security.jgss</addModule>
                        <addModule>java.sql</addModule>
                        <addModule>java.xml</addModule>
                        <addModule>java.xml.crypto</addModule>
                        <addModule>jdk.jfr</addModule>
                        <addModule>jdk.unsupported</addModule>
                        <addModule>jdk.zipfs</addModule>
                        <addModule>jdk.crypto.cryptoki</addModule>
                        <addModule>jdk.crypto.ec</addModule>
                        <addModule>jdk.management</addModule>
                        <addModule>jdk.management.agent</addModule>
                    </addModules>

                    <noHeaderFiles>true</noHeaderFiles>
                    <noManPages>true</noManPages>
                    <stripDebug>true</stripDebug>
                    <compress>2</compress>
                </configuration>
            </plugin>

            <!-- For packaging (jpackage) we run the jpackage executable from the JDK inside a profile (see profiles below). -->
        </plugins>
    </build>

    <profiles>
        <!-- Windows packaging profile: sets platform classifier and runs jpackage during package phase -->
        <profile>
            <id>windows-package</id>
            <activation>
                <property>
                    <!-- you can manually enable with -Pwindows-package -->
                    <name>env.WIN</name>
                </property>
            </activation>
            <properties>
                <javafx.platform>win</javafx.platform>
            </properties>

            <build>
                <plugins>
                    <!-- Exec plugin to call jpackage (only activated inside windows-package) -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>jpackage</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <!-- Use JAVA_HOME on the build machine. Ensure JAVA_HOME points to JDK 21+ with jpackage. -->
                                    <executable>${env.JAVA_HOME}\bin\jpackage.exe</executable>
                                    <workingDirectory>${project.build.directory}</workingDirectory>
                                    <arguments>
                                        <argument>--type</argument><argument>msi</argument>

                                        <!-- runtime image created by maven-jlink-plugin -->
                                        <argument>--runtime-image</argument><argument>${project.build.directory}\runtime</argument>

                                        <argument>--dest</argument><argument>${project.build.directory}\installer</argument>

                                        <argument>--name</argument><argument>${app.name}</argument>
                                        <argument>--app-version</argument><argument>${app.version}</argument>
                                        <argument>--vendor</argument><argument>${app.vendor}</argument>

                                        <!-- The input directory contains the main jar and dependency jars -->
                                        <argument>--input</argument><argument>${project.build.directory}\installer-input</argument>

                                        <!-- our main jar -->
                                        <argument>--main-jar</argument><argument>${project.build.finalName}.jar</argument>
                                        <argument>--main-class</argument><argument>${main.class}</argument>

                                        <argument>--icon</argument><argument>src\main\resources\icon.ico</argument>

                                        <!-- Windows-specific options -->
                                        <argument>--win-shortcut</argument>
                                        <argument>--win-menu</argument>
                                        <argument>--win-dir-chooser</argument>
                                        <argument>--win-per-user-install</argument>

                                        <!-- example JVM options; include library path for legacy native lookup as fallback -->
                                        <argument>--java-options</argument><argument>-Xmx512m</argument>
                                        <argument>--java-options</argument><argument>-Dfile.encoding=UTF-8</argument>
                                        <argument>--java-options</argument><argument>-Djava.library.path=.</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
